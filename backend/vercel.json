{
  "functions": {
    "api/**/*.ts": {
      "maxDuration": 10,
      "memory": 512,
      "includeFiles": "dist/**"
    }
  },
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/index.ts" }
  ]
}
